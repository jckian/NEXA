# 260707 P2 report — Depth discipline (band depth normalization) — **STOPPED per §5**

Executor: opus. Outcome: **NO EDIT to program-massing-shortfloor.html.** P2 as specified cannot satisfy its own acceptance; §5 STOP condition #1 ("SAMPLE rects change and you cannot make the phase a SAMPLE-no-op → STOP, show the diff") is hit. Evidence below. Real files (HTML, replica3.js, baselines, sample.txt) are byte-untouched; only scratchpad copies were used.

## The blocker: the plan's "SAMPLE is a no-op" premise is factually false for the frozen sample.txt

The rule (small room ≤200 m² → dMod = band's modal depth D, wMod = ceil(cells/D)) was implemented verbatim in a scratch replica and diffed against the current layout:

```
sample.txt     orig FW/FD 29/17 → P2 26/17   rect diff: 44 rects moved  => CHANGED (NOT byte-identical)
case_all.txt   orig FW/FD 27/16 → P2 26/16   rect diff: 44 rects moved; FD UNCHANGED (16)
```

SAMPLE bands are NOT near-uniform: nearly every band has small rooms whose depth deviates from the band mode (studio apts d3 on a d5 band, mechanical d6 on a d5 band, electrical d3 on a d8 band, etc.), so the rule reshapes SAMPLE substantially → HARD constraint (a) violated.

## No tightening can reconcile it (deviation-threshold sweep, rooms that change dimension)

Only normalize when |dMod − D| ≥ T. A room-dimension change is the exact predictor of loss of byte-identity (unchanged dims ⇒ unchanged band widths ⇒ identical packing):

```
T   SAMPLE-changed   case_all-changed
1        24                13
2        17                 5
3         2                 2
4         1                 1
5         1                 0
6         0                 0     ← SAMPLE no-op reached, but case_all also does NOTHING
```

SAMPLE's max intra-band deviation (5, electrical d3→d8 on L-2N) strictly dominates case_all's (4, electrical d3→d7). So the smallest T that spares SAMPLE (T=6) also spares all of case_all. Direction-only (trim-deep / widen-shallow) and dominant-mode gating fail identically — SAMPLE has deviations in both directions and on high-dominance bands (L3S [5,5,3,3,5,5,3] 57%, L-2N [8,8,3] 67%), which are denser than case_all's comparable bands. **The two datasets are statistically the same shape; any rule sensitive enough to act on case_all acts on SAMPLE.**

## Even ignoring SAMPLE, the case_all goal is unreachable at the ≤200 gate

Target FD ≤ 13; measured FD stays **16** under the full (untightened) rule. FD = southDepth+1+northDepth is pinned by rooms ABOVE the gate, exactly as the plan warned:
- north: `office` 392 m² → d8 (L1)
- south: `parking bay group` 322 m² → d7 (7×7, L-2)
Both > 200 m², so the gate never touches them; 8+1+7 = 16. Pulling FD to ≤13 would require normalizing >200 rooms — the plan permits a bounded gate-widening ONLY "if SAMPLE stays a no-op," which it does not. So widening is disallowed → STOP.

Net: for case_all the gate only performs ±1 small-room shuffles (the big depth lumps — office d8, parking d7, plate d11 — all survive), so the visual "constant-depth strip" gain is marginal while the SAMPLE cost is a hard regression.

## Verification state (all baselines intact, nothing regressed because nothing changed)

- `node replica3.js` (unmodified): case_all PASS .862/.857/1.000 FD16 corridorΣ185 conn42/42; sample PASS .788/.957 corridorΣ178 conn63/63; learning PASS conn33/33. (== post-P1.5 baseline.)
- `node harness.js` → **166 passed, 0 failed**.
- No BACKUP/…-preP2.html created (no edit made). HTML + replica3.js mtimes unchanged.

## Recommendation to commander (scope decision needed)

P2 is infeasible as written against the frozen sample.txt. Options:
1. **Relax SAMPLE for P2** the same way §0.5 relaxed P1 (accept overlap ≥.75 instead of byte-identity). Then the literal rule can ship; case_all bands become more depth-uniform among small rooms (still FD 16). Recommend re-measuring SAMPLE overlaps after.
2. **Redefine the depth target** to attack the real FD driver: normalize the >200 depth-setting rooms (office d8, parking d7) per band — this is what actually pulls FD toward ≤13, but it is a bigger reshaping and also breaks SAMPLE byte-identity.
3. **Retire P2** — case_all bands are already contiguous (MAX intra-band gap 0, bbox fill 1.000); the depth heterogeneity is a Z-axis reading issue that the ≤200 gate barely improves. If the bar-reading is acceptable in the p1_5-caseall screenshot, P2's marginal gain may not justify the SAMPLE risk.

Prefer (1) or (3). Awaiting a scope call before any edit.

## Scratch artifacts (safe to delete)
`scratchpad/replica3_p2.js`, `scratchpad/p2diff.js` — experiment only; not in the repo tools dir.
