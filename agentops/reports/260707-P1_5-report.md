# 260707 P1.5 report — Block subdivision (max side 34 ft)

Executor: opus. Scope: `program-massing-shortfloor.html` layout stage only, downstream of `computeLayout`. Verified against plan §P1.5 acceptance (a)-(d) + §1 hard constraints.

## Provenance (important)

P1.5 was **already implemented** in the working file by a prior, unreported invocation of this task: `subdivideBlock` is ABSENT from `BACKUP/…-preP1.html` and `…-preLayoutFix.html` but PRESENT in the current file. **I made ZERO edits this session** — my work was end-to-end verification + this report. The genuine pre-P1.5 revert point is therefore `BACKUP/program-massing-shortfloor-260707-preP1.html`; my checkpoint copy `BACKUP/program-massing-shortfloor-260707-preP1_5.html` was cut from the already-implemented file (contains P1.5), so do NOT use it to revert P1.5.

## What the implementation does (file:line)

`program-massing-shortfloor.html`:
- `MAX_SIDE_MOD = 4` (:583) — single named constant, 4 × 8'-6" = 34'-0". Comment at :580-582 documents the strict-34ft flip.
- `subdivideBlock(r)` (:515-531) — SHORT-exempt; returns `[r]` if wMod≤4 && dMod≤4; else `splitDim(n)` = ceil(n/4) parts, `base=floor(n/parts)` with `+1` on first `rem` (→ 5=3+2, 7=4+3, 11=4+4+3, never a 1-sliver); tiles the cells, each block inherits `...r` (type/category/level/side) with `area = parent.area × (wi·dj)/totalCells` (Σ conserved).
- Applied at (:512): `for (const [lv,rects] of floorRects) floorRects.set(lv, rects.flatMap(subdivideBlock))` — after all packing/corridor/plate emission, immediately before `return`. Program rects AND plate-slab rects are split; SHORT rects and core shafts (live in `coreShafts`, not `floorRects`) are exempt.

## MAX_SIDE_MOD location + strict-34ft ambiguity

Constant at :583. User wrote "<34ft". Current value 4 = 34'-0" exactly (interprets "up to 34ft"). If "strictly under 34ft" is meant, flip the ONE constant to 3 (= 25'-6"). Left at 4 pending user confirmation — one-edit change, documented in-code at :580-582.

## Acceptance

(a) **Rect audit** (scratchpad `audit_p1_5.js`, applies the HTML's verbatim `subdivideBlock` to replica3's emitted rects — replica layout == HTML layout per P1): all 3 cases **PASS** — max block wMod=4, dMod=4; 0 oversized program blocks; 0 slivers; worst per-room area error ≤5.7e-14 m² (float noise, ≪0.5). Block counts confirm splitting: case_all 61 parents→126 blocks, sample 77→132, learning 55→141.

(b) **replica3 identical to post-P1** (replica3.js UNCHANGED — it has no `subdivideBlock`, yet still matches, proving subdivision is purely downstream of computeLayout; assumption verified explicitly):
```
case_all  FW27 FD16 cores@0,23 corridorΣ185  overlap .862/.857/1.000  conn 42/42  [ASSERT]…=> PASS
sample    FW29 FD17 cores@0,18 corridorΣ178  overlap .788/.957         conn 63/63  [ASSERT]…=> PASS
learning  FW25 FD15 cores@0,9  corridorΣ126  overlap 1.000             conn 33/33  [ASSERT]…=> PASS
```
All metrics byte-match the post-P1 baseline. Footprints did not move.

(c) **Screenshots** (session-log/260707-tools/): `p1_5-caseall.png`, `p1_5-sample.png`, `p1_5-caseall-structure.png`.
- case=all: silhouette + stats identical to `p1-caseall.png` (GFA 12,001, 10 floors, 95 spaces, 4 SHORT L1-L4); former mega-plates now show clear grid seams. ✔
- SAMPLE: coherent weave intact, seams visible on the larger blue plates; stats HARD unchanged. ✔
- structure `&mode=structure` (budget 30000): full adaptive frame renders — columns/girders/slabs/curtain-wall + SHORT kit granulated to individual 8'-6" bricks + core shafts; fully-rendered scene ⇒ zero fatal console errors. ✔ (The visible per-cell SHORT bricks confirm not double-splitting SHORT was correct.)

(d) **harness** `node harness.js` → **166 passed, 0 failed**.

## SAMPLE regression check (HARD)

Total GFA **14,272 m²**, **3 SHORT floors L1-L3** — unchanged (screenshot + replica). computeShortFloors / SAMPLE text / classifiers untouched.

## Noted, not fixed

- **Corridors are not subdivided.** `floorCorr` rects (dMod=1 circulation spines) can have wMod>4 but are excluded from the §512 loop (only `floorRects` is iterated). Defensible: plan enumerates "program rects AND plate-slab rects; core shafts exempt" — corridors are 1-module-deep circulation, not "量體/mass", so one side is already ≤4. Flagging in case the directive is meant to cover them too (would be a one-line addition to also flatMap floorCorr).
- **Backup naming caveat** (see Provenance): `…-preP1_5.html` already contains P1.5; revert P1.5 from `…-preP1.html`.

Cleanup done: `_handoff-test.html` deleted, http.server 8099 stopped, edge profiles cleaned.
